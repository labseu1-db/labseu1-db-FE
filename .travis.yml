language: node_js
node_js: "10.10.0"
install:
  - yarn
script:
  - npm ls babel-jest
  - yarn test
  - yarn run build
deploy:
  provider: firebase
  token: $FIREBASE_TOKEN
  skip-cleanup: true
  local_dir: build
  on:
    branch: master
