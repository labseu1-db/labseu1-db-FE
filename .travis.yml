language: node_js
node_js: node
cache: yarn

install:
  - yarn install

script:
  - yarn test
  - yarn run build

deploy:
  provider: pages       
  skip-cleaning: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master